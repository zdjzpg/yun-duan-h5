/**
 * 系统配置文件
 * 
 * 📚 本文件包含系统级通用配置，可根据实际业务需求修改
 * 
 * 配置内容：
 * - 系统名称与 Logo
 * - 默认路由
 * - 角色定义与标签
 * 
 * @module config/system
 */

// ==================== 系统基本信息 ====================

/**
 * 系统名称配置
 * 在登录页、顶部导航等位置显示
 */
export const SYSTEM_CONFIG = {
  /** 系统全称 */
  name: '景区剧场管理系统',
  
  /** 系统简称（侧边栏收起时显示） */
  shortName: '剧场',
  
  /** 系统描述（登录页副标题） */
  description: '专业的景区 SaaS 剧场业务管理平台',
} as const;

// ==================== 路由配置 ====================

/**
 * 默认路由配置
 */
export const ROUTE_CONFIG = {
  /** 登录成功后的默认跳转路径 */
  defaultRoute: '/dashboard/theater/venues',
  
  /** 登录页路径 */
  loginRoute: '/auth/login',
} as const;

// ==================== 角色与权限配置 ====================

/**
 * 系统角色类型
 * 
 * 🔒 权限设计原则：
 * - admin: 系统管理员，拥有所有权限
 * - manager: 业务管理者，负责核心业务管理
 * - staff: 普通员工，执行日常业务操作
 * - viewer: 只读用户，仅可查看数据
 */
export type UserRole = 'admin' | 'manager' | 'staff' | 'viewer';

/**
 * 角色显示标签
 * 用于 UI 展示
 */
export const ROLE_LABELS: Record<UserRole, string> = {
  admin: '管理员',
  manager: '经理',
  staff: '员工',
  viewer: '查看者',
};

/**
 * 角色颜色映射
 * 对应 Ant Design Tag 预设颜色
 */
export const ROLE_COLORS: Record<UserRole, string> = {
  admin: 'red',       // 管理员 - 红色（最高权限）
  manager: 'blue',    // 经理 - 蓝色（管理权限）
  staff: 'green',     // 员工 - 绿色（执行权限）
  viewer: 'default',  // 查看者 - 灰色（只读）
};

/**
 * 获取角色标签
 */
export function getRoleLabel(role: UserRole): string {
  return ROLE_LABELS[role];
}

/**
 * 获取角色颜色
 */
export function getRoleColor(role: UserRole): string {
  return ROLE_COLORS[role];
}