> 本文档从 `景区剧场业务/src/pages/dashboard/theater/venues/LOCK_STATUS_TOOLTIP.md` 精简迁移，仅保留“锁定状态”表头提示的交互说明。当前 H5 场馆列表已展示锁定状态基础信息，气泡文案和交互可按需在后续版本补充。

核心含义：

- 当场馆的座位结构已被演出使用后，系统会自动锁定该场馆，避免修改座位布局导致已售票数据异常。
- 锁定后：
  - 不允许修改座位图（座位、座区、楼层、舞台等结构）
  - 仍允许修改场馆基础信息（名称、地址等）
  - 如需调整座位结构，需通过“复制场馆”创建新版本后重新配置

